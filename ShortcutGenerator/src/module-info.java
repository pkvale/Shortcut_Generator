/**
 * This program is creating a searchable list of shortcuts based on a text file.  The text file is formatted in the following 
 * format: -shortcut: #searchTerm1, #searchTerm2, #searchTerm3, #searchTerm4, etc., *Description
 * Example: (eclipse) -Ctrl+S: #save
 */
/**
 * @author peter kvale
 * 1/28/21
 */
module shortcutGenerator {
	/*
	 * import text file
	 */

	/*
	 * create hash table
	 * 	-creates a hash table of objects with names as shortcuts and with properties as the search terms and descriptions
	 */

	/*
	 * Start GUI 
	 * 	-user input box
	 * 	-output popup
	 * 	-10 recent searches
	 * 	-save searches
	 * 	-load searches
	 */

	/*
	 * declare search function 
	 * findSearchedShortcuts array[objects](hashtable myHashtable){
	 * }
	 */
	
	/*
	 * declare search function 
	 * getSearchedDescriptions array[objects](hashtable myHashtable){
	 * }
	 */

	/*
	 * declare load function 
	 */
	
	/*
	 * declare save function 
	 */
}